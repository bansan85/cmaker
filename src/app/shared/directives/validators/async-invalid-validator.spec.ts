//import { AsyncInvalidValidator } from './async-invalid-validator';

describe('AsyncInvalidValidator', () => {
  it('should create an instance', () => {
    // const directive = new AsyncInvalidValidator();
    // expect(directive).toBeTruthy();
  });
});
