//import { InvalidValidator } from './invalid-validator';

describe('InvalidValidator', () => {
  it('should create an instance', () => {
    //const directive = new InvalidValidator();
    //expect(directive).toBeTruthy();
  });
});
