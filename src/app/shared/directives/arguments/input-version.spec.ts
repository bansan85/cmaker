//import { InputVersion } from './input-version';

describe('InputVersion', () => {
  it('should create an instance', () => {
    // const directive = new InputVersion();
    // expect(directive).toBeTruthy();
  });
});
