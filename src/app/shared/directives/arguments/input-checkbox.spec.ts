import { InputCheckbox } from './input-checkbox';

describe('InputCheckbox', () => {
  it('should create an instance', () => {
    // const directive = new InputCheckbox();
    // expect(directive).toBeTruthy();
  });
});
