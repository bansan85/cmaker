//import { CMakeCommandInterface } from './cmake-command-interface';

describe('CmakeCommandInterface', () => {
  it('should create an instance', () => {
    //expect(new CMakeCommandInterface()).toBeTruthy();
  });
});
