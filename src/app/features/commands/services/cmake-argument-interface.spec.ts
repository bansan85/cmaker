//import { CMakeArgumentInterface } from './cmake-argument-interface';
import { describe, it } from 'vitest';

describe('CmakeArgumentInterface', () => {
  it('should create an instance', () => {
    //expect(new CMakeArgumentInterface()).toBeTruthy();
  });
});
